# Tomika ゴルフレッスン予約・管理システム 仕様書

作成日: 2026年4月25日

---

## 1. システム概要

個人ゴルフスクール向けのWebアプリケーション。顧客がLINEアカウントで認証し、レッスンの予約・マイページ閲覧・AIカルテ確認が行える。管理者はダッシュボードから予約管理・顧客管理・AIカルテ作成が行える。

---

## 2. 使用技術スタック

| 区分 | 技術 | バージョン |
|------|------|----------|
| フレームワーク | Next.js (App Router) | 16.2.2 |
| 言語 | TypeScript | 5 |
| UIライブラリ | React | 19.2.4 |
| スタイリング | Tailwind CSS | 4 |
| データベース | Supabase (PostgreSQL) | - |
| 認証 | Supabase Auth + LINE LIFF | - |
| 外部カレンダー | Google Calendar API | - |
| AI生成 | Anthropic Claude API (claude-haiku-4-5) | - |
| 日付処理 | date-fns / date-fns-tz | 4.1.0 / 3.2.0 |

---

## 3. 外部サービス一覧

| サービス名 | 用途 | 認証方式 |
|-----------|------|--------|
| Supabase | DB・ユーザー認証 | Anon Key (RLS) / Service Role Key |
| LINE LIFF | 顧客のモバイル認証 | LIFF ID + LINE IDトークン |
| Google Calendar | レッスン枠・稼働時間の管理 | Googleサービスアカウント（秘密鍵） |
| Anthropic (Claude) | AIカルテの自動生成 | APIキー |

---

## 4. データベース設計（Supabase / PostgreSQL）

### 4-1. profiles テーブル（ユーザー情報）

| カラム名 | 型 | 説明 |
|---------|---|------|
| id | UUID (PK) | Supabase Auth の user.id と同一 |
| line_user_id | TEXT (UNIQUE) | LINE の userId |
| name | TEXT | 氏名（初回登録時に入力） |
| name_kana | TEXT | フリガナ |
| phone | TEXT | 電話番号 |
| role | TEXT | 'customer' または 'admin' |
| ticket_man_to_man | INTEGER | マンツーマンチケット残数 |
| ticket_group | INTEGER | グループチケット残数 |
| admin_memo | TEXT | 管理者用メモ |
| created_at | TIMESTAMP | 作成日時 |

### 4-2. reservations テーブル（予約情報）

| カラム名 | 型 | 説明 |
|---------|---|------|
| id | UUID (PK) | 予約ID |
| user_id | UUID (FK → profiles.id) | 予約ユーザー |
| status | TEXT | 'confirmed' / 'completed' / 'cancelled' |
| lesson_type | TEXT | 'man-to-man' / 'group' |
| start_time | TIMESTAMP | レッスン開始日時 |
| end_time | TIMESTAMP | レッスン終了日時 |
| options | JSONB | 予約オプション（将来拡張用） |
| customer_memo | TEXT | 顧客からのメモ |
| created_at | TIMESTAMP | 作成日時 |

### 4-3. review_notes テーブル（AIカルテ）

| カラム名 | 型 | 説明 |
|---------|---|------|
| id | UUID (PK) | カルテID |
| reservation_id | UUID (FK → reservations.id, UNIQUE) | 対応する予約 |
| video_url | TEXT | レッスン動画のURL |
| karte_good | TEXT | 良かった点（AI清書版） |
| karte_improve | TEXT | 改善点 |
| karte_homework | TEXT | 宿題・次回のポイント |
| is_draft | BOOLEAN | 下書きフラグ |
| created_at | TIMESTAMP | 作成日時 |

### セキュリティ

- **Row Level Security (RLS)** を有効化
- 顧客は自分のプロフィール・予約・カルテのみ参照・操作可能
- 管理者操作はサーバーサイドの Service Role Key 経由で実行

---

## 5. 認証フロー

### 顧客認証（LINE LIFF）

```
1. ユーザーがLINEアプリ内でWebページを開く（LIFF起動）
2. LINE ログイン確認 → 未ログインなら LINE の認証画面へリダイレクト
3. LINE プロフィール（displayName, userId）を取得
4. LINE の OIDC スコープで署名済み IDトークン（JWT）を取得
5. サーバー API (/api/auth/line-session) に IDトークンを送信
   → LINE userId のハッシュ値からメール・パスワードを生成
   → Supabase Auth にユーザー作成（初回）またはサインイン（既存）
   → Supabase の access_token / refresh_token を返却
6. プロフィール同期 (/api/auth/sync)
   → profiles テーブルに line_user_id を記録
   → name が未登録なら名前登録ページ (/register) へ強制リダイレクト
```

### 管理者認証

- 環境変数でユーザー名・パスワードを管理（最大2アカウント）
- ログイン時に **HMAC-SHA256** で署名したトークンを **httpOnly Cookie** に保存
- Next.js Middleware が `/dashboard/*` と `/api/admin/*` へのすべてのアクセスをトークン検証

---

## 6. 主要機能

### 6-1. 予約システム（顧客向け）

**空き枠の生成ロジック**
- Google Calendar の「稼働」「レッスン」「マンツーマン」「グループ」キーワードを含むイベントを60日先まで取得
- 1つの稼働ブロック（例：3時間）を時間ごとに分割して空き枠を生成：
  - **マンツーマン用（50分枠）**: 毎時0分開始
  - **グループ用（25分枠）**: 毎時0分・30分開始
- 既存予約との重複判定 → `isAvailable: true/false`
- 開始まで3時間未満の枠は予約不可

**予約の作成・キャンセル**
- ダブルブッキング防止チェックあり
- 予約後は `confirmed` ステータスで保存
- キャンセルは `cancelled` ステータスに変更

### 6-2. AIカルテ機能（管理者向け）

1. 管理者がレッスン後にメモ（手書き文字の音声入力 or テキスト入力）を入力
2. 「AIで清書」ボタンを押すと **Claude API (claude-haiku-4-5)** へ送信
3. Claudeがゴルフインストラクター視点で顧客向けの温かい文章に清書（400〜600文字）
4. 管理者が内容を確認・編集後に「保存」
5. 顧客のマイページから閲覧可能になる（動画URLも添付可能）

**AIプロンプトの構成（5段階）**
> 感謝の言葉 → レッスン総評 → 改善点 → 宿題 → 応援メッセージ

### 6-3. チケット管理

- マンツーマン用・グループ用の2種類のチケット
- 管理者ダッシュボードから付与・削除が可能
- 予約完了処理時に自動で1枚消費

### 6-4. 代理予約（管理者向け）

- 管理者が顧客に代わって予約を作成できる機能
- 顧客一覧から対象ユーザーを選択して予約操作

---

## 7. ページ・ルート構成

### 顧客向けページ

| URL | ページ名 | 機能 |
|-----|---------|------|
| `/` | ランディングページ | サービス紹介・料金・予約CTA |
| `/register` | 名前登録 | 初回ユーザーの名前・フリガナ・電話番号入力 |
| `/booking` | 予約カレンダー | 日付→時間→レッスンタイプ選択→予約確定 |
| `/mypage` | マイページ | 予約一覧・チケット残数・キャンセル |
| `/mypage/karte/[id]` | カルテ詳細 | AIカルテ・動画の閲覧 |
| `/privacy` | プライバシーポリシー | - |
| `/terms` | 利用規約 | - |

### 管理者向けページ

| URL | ページ名 | 機能 |
|-----|---------|------|
| `/dashboard/login` | 管理者ログイン | - |
| `/dashboard` | ダッシュボード | 今日以降の予約一覧・完了処理 |
| `/dashboard/customers` | 顧客管理 | 全顧客一覧・チケット管理・メモ編集 |
| `/dashboard/reservations/[id]/karte` | カルテ作成・編集 | AIカルテの作成・保存 |
| `/dashboard/booking` | 代理予約 | 管理者による予約作成 |
| `/dashboard/ghost-account` | テストアカウント作成 | 開発・テスト用 |

### APIエンドポイント

| メソッド | パス | 機能 |
|---------|------|------|
| GET | `/api/calendar/slots` | 空き枠一覧取得 |
| POST | `/api/booking` | 予約作成 |
| POST | `/api/booking/cancel` | 予約キャンセル |
| GET | `/api/user/profile` | ユーザー情報取得 |
| GET | `/api/user/karte` | カルテ一覧取得 |
| PATCH | `/api/user/name` | 名前更新 |
| POST | `/api/auth/line-session` | LINE → Supabase 認証 |
| POST | `/api/auth/sync` | プロフィール同期 |
| POST | `/api/admin/login` | 管理者ログイン |
| POST | `/api/admin/logout` | 管理者ログアウト |
| GET/PATCH | `/api/admin/reservations` | 予約管理 |
| GET/PATCH | `/api/admin/customers` | 顧客管理 |
| POST | `/api/admin/karte/generate` | AIカルテ生成 |
| POST | `/api/admin/karte/save` | カルテ保存 |
| POST | `/api/admin/tickets` | チケット付与 |

---

## 8. ディレクトリ構造（主要部分）

```
src/
├── app/
│   ├── (customer)/          # 顧客向けページ群
│   │   ├── booking/
│   │   ├── mypage/
│   │   └── register/
│   ├── (admin)/             # 管理者向けページ群
│   │   └── dashboard/
│   ├── api/                 # APIルート（バックエンド）
│   └── page.tsx             # ランディングページ
├── lib/
│   ├── supabase/            # Supabaseクライアント
│   ├── google-calendar.ts   # Google Calendar連携
│   └── admin-token.ts       # 管理者トークン署名・検証
├── hooks/
│   └── useAuth.ts           # LINE LIFF認証フック
├── contexts/
│   └── AuthContext.tsx      # 認証状態の全体管理
├── components/              # 共通コンポーネント
└── utils/
    ├── ai-prompts.ts        # Claudeプロンプト定義
    ├── booking-rules.ts     # 予約ルール判定
    └── slot-slicer.ts       # 空き枠分割ロジック
supabase/
└── migrations/
    └── 01_initial_schema.sql  # DBスキーマ定義
```

---

## 9. 環境変数（必要なもの）

```
# Supabase
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY

# Google Calendar
GOOGLE_CLIENT_EMAIL
GOOGLE_PRIVATE_KEY
GOOGLE_CALENDAR_ID

# LINE LIFF
NEXT_PUBLIC_LIFF_ID
LINE_CHANNEL_ID
LINE_CHANNEL_SECRET

# Anthropic Claude API
KARTE_CLAUDE_API_KEY

# 管理者認証
ADMIN_SECRET
ADMIN_1_USERNAME
ADMIN_1_PASSWORD
ADMIN_2_USERNAME
ADMIN_2_PASSWORD
```

---

## 10. システム構成図（概念）

```
[顧客（LINEアプリ）]
       ↓ LIFF起動
[Next.js フロントエンド]
       ↓ API呼び出し
[Next.js API Routes]
    ↙         ↘
[Supabase]  [Google Calendar]
(DB・Auth)  (稼働枠・予約枠管理)
       ↘
   [Claude API]
   (カルテAI生成)

[管理者（ブラウザ）]
       ↓ Cookie認証
[Next.js 管理画面]
       ↓
[Next.js API Routes /api/admin/*]
       ↓
[Supabase（Service Role Key）]
```

---

*この仕様書は 2026年4月25日時点のシステム状態を元に作成しています。*
